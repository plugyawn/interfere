"""Training metrics helpers (S1 stub)."""
from __future__ import annotations

from typing import Dict


def accuracy_from_logits(logits, targets, mask=None) -> Dict[str, float]:
    return {"acc": 0.0}

